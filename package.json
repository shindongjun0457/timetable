{
  "name": "ohyun-timetable",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --local",
    "build:data": "node ./scripts/fetch-timetable.mjs",
    "build:data:force": "node ./scripts/fetch-timetable.mjs --force",
    "build": "npm run build:data:force",
    "preview": "wrangler pages dev public --local"
  },
  "dependencies": {
    "comcigan-parser": "1.0.0",
    "dayjs": "^1.11.13"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
